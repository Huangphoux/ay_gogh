from starhtml import Relay

relay = Relay()

# Remember to change this in the Relay class
# elif isinstance(event, ElementEvent):
#     yield elements(event.element, event.selector, event.mode,  use_view_transition=True)
