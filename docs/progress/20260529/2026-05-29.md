- Viết xong báo cáo chương 1 và chương 2
- Chỉnh sửa bố cục cho khớp báo cáo mẫu
- Thêm .env
- Sửa lỗi
  - `if " " in word` → `if not word.isalpha()`: kiểm tra nếu có kí tự ngoài bảng chữ cái
  - Những từ được highlight bị highlight nhiều lần, thành ra nằm trong `<span>` lồng nhau, do không kiểm tra từ đó đã được highlight chưa
- Bật tắt highlight và chú thích trong trang đọc. Lưu cài đặt vào CSDL và áp dụng trên tất cả các trang
  - Tắt highlight sẽ hiện gạch chân để phân biệt với những từ chưa có trong từ điển cá nhân
- Cải thiện giao diện trên màn hình nhỏ
- Nút `Review anyway`: bỏ qua việc phải đợi mới được ôn tập từ vựng
- Các từ số đếm, ngày trong tuần, tên tháng cũng được xem là trong danh sách NGSL nhưng không có cấp độ
- Các từ có trong từ điển cá nhân nằm trong `<span>` sẵn, từ đó cho phép người dùng click vào để tra từ đó, không cần phải bôi đen
- Sử dụng Popover API: giải pháp cũ chỉ có thể gắn popup vào viền trên màn hình, không thể gắn xuống viền dưới; không thể hạ độ tối cho nền phía sau của popup